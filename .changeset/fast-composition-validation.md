---
"@weaver-conf/config-engine": patch
"@weaver-conf/weaver-server": patch
---

Remove fixed composition-validation overhead from ordinary configuration checks while preserving composition and graph-safety semantics. Bump the server so its workspace dependency delivers the config-engine runtime fix to server consumers.
