---
"@weaver-conf/config-types": major
"@weaver-conf/config-engine": major
"@weaver-conf/weaver-server": major
---

Enforce supported recursive registration grammar, deliver absence-only materialized defaults, and compose declared fragment slots with strict registered membership across schema boundaries (PR152: weaver-xekg, weaver-5bgr, weaver-7tx7).

Share regex safety between registration and runtime matching, validate defaults against overlapping member constraints, reject marker-bearing defaults, and use one contextual defaulted candidate across governing schemas for reads, effective checks, removals and publication.

Conservatively validate dynamic-pattern default combinations, reject markers synthesized by default application, and enforce a final recursive no-marker guard on resolved delivery candidates.
